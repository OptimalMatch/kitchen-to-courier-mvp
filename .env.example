# Copy to .env. The release to pin: the archive unidatum-<version>-linux-amd64.tar.gz must be in this folder.
UNIDATUM_VERSION=v2.362.0
# The SQL wire for the dashboards (Metabase logs in with these).
SQLD_USER=demo
SQLD_PASSWORD=demo-only-change-me
